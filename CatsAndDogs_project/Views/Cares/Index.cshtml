@model IEnumerable<CatsAndDogs_project.Models.Care>

@{
    ViewData["Title"] = "Index";
}

<head>
    <link href="~/css/index_forstore1.css" rel="stylesheet" />
</head>

@*<h1>Index</h1>

    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Description)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Tip)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Price)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Image)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Type)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Category)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
    @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tip)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Image)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Type)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Category.Name)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
    }
        </tbody>
    </table>*@


<h1>טיפוח</h1>

@if (ViewContext.HttpContext.User.IsInRole("Admin") || ViewContext.HttpContext.User.IsInRole("Editor"))
{
    <p>
        <a class="create_btn" asp-action="Create">הוספת מוצר טיפוח חדש</a>
    </p>
}

<p class="choose" asp-action="DorOrCat">

    @*בחר את סוג החיה:*@ <select name="animal" id="animalid">

        <option value="" selected disabled hidden>בחר</option>
        <option value="" selected="selected">כלב</option>
        <option value="" selected="selected">חתול</option>
    </select>

    @*בחר את הקטגוריה הרצויה:*@ <select name="cate" id="categ">

        <option value="" selected disabled hidden> בחר קטגוריה </option>
        @*<option value="" selected disabled hidden>בחר</option>*@
        <option value="" selected="selected">שמפו</option>
        <option value="" selected="selected">מברשות</option>
        <option value="" selected="selected">בגדים</option>
    </select>


    @*מחיר:*@ <input name="num1" placeholder="מחיר החל מ" autocomplete="off" />

    <input name="num2" placeholder="מחיר עד" autocomplete="off" />

    <input id="sub_search" type="submit" value="חיפוש" />
</p>

@*<div class="sidenav">
        <h4> קטגוריות לטיפוח</h4>
        <hr />
        <a href="#">שמפו</a>
        <a href="#">מברשות</a>
        <a href="#">בגדים</a>

    </div>*@

<div class="container">
    <div class="row">

        @foreach (var item in Model)
        {
            <div class="card col-3" style="width: 18rem; position:inherit">
                <img src="@item.Image" class="card-img-top" alt="@item.Name">
                <div class="card-body">
                    <h5 class="card-title"> @item.Name</h5>
                    <p class="card-text" style="overflow: hidden; margin-bottom: -19px; text-overflow: ellipsis;white-space: nowrap;">@item.Description</p>
                    <p> <h5> @Html.DisplayNameFor(model => model.Tip) </h5> @item.Tip </p>
                    <p id="price"> @Html.DisplayNameFor(model => model.Price): @item.Price ₪ </p>

                </div>
                <div class="option">
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-primary">לפרטים נוספים</a>
                    <a class="editOption" asp-action="Edit" asp-route-id="@item.Id">עריכה</a>
                    <a class="removeOption" asp-action="Delete" asp-route-id="@item.Id">מחיקה</a>
                </div>
            </div>

        }

    </div>
</div>



@*@section Scripts {
        @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
        <link href="~/css/index_1.css" rel="stylesheet" />
    }*@
